version: 2
jobs:
  build:
    docker:
      - image: circleci/node:14

    steps:
      - checkout

      - run: cd frontend-react && npm install
workflows:
  version: 2.1
    node-tests:
      jobs:
        - node/test